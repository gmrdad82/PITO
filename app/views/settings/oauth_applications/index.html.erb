<% content_for(:title, "oauth applications") %>
<% breadcrumb(["settings", settings_path], "oauth applications") %>

<div style="margin-bottom: 8px;">
  <h1>oauth applications</h1>
  <p class="text-muted" style="margin-top: 2px;">
    third-party clients (e.g. the pito CLI) authenticate via authorization
    code + PKCE. <code>client_id</code> and <code>client_secret</code> are
    shown exactly once at creation. revoking an application also revokes
    every access and refresh token it has issued.
  </p>
</div>

<div class="dot-list" style="margin-bottom: 12px;">
  <%= render(BracketedLinkComponent.new(label: "new application", href: new_settings_oauth_application_path)) %>
</div>

<% if @applications.any? %>
  <table>
    <thead>
      <tr>
        <th>name</th>
        <th>client_id</th>
        <th>redirect_uri</th>
        <th>scopes</th>
        <th>confidential</th>
        <th class="num">created</th>
        <th class="col-action"></th>
      </tr>
    </thead>
    <tbody>
      <% @applications.each do |app| %>
        <tr>
          <td><%= link_to app.name, settings_oauth_application_path(app) %></td>
          <td><code><%= app.uid %></code></td>
          <td style="max-width: 240px; white-space: normal;">
            <% app.redirect_uri.to_s.split("\n").each do |uri| %>
              <code><%= uri %></code><br>
            <% end %>
          </td>
          <td style="max-width: 240px; white-space: normal;"><code><%= app.scopes %></code></td>
          <td><%= app.confidential? ? "yes" : "no" %></td>
          <td class="num"><%= app.created_at.strftime("%Y-%m-%d") %></td>
          <td class="col-action">
            <%= render(BracketedLinkComponent.new(label: "revoke", href: revoke_settings_oauth_application_path(app), destructive: true)) %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p class="text-muted">no applications yet. click <code>[ new application ]</code> to register one.</p>
<% end %>
