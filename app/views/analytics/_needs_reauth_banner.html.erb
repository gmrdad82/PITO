<%# Phase 13.3 — `needs_reauth` banner. Renders only when the
    connection's `needs_reauth` flag is true. Copy locked by
    master-agent decision 6. %>
<div class="needs-reauth-banner caption"
     style="border: 1px solid var(--color-border); padding: 8px 10px; margin: 8px 0;">
  re-authorize this channel to continue syncing analytics.
</div>
