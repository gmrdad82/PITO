<%# Phase 13.3 — Data-freshness line. Renders at the top of every
    analytics dashboard. Local: `timestamp` (Time or nil). %>
<p class="caption analytics-data-freshness" style="margin: 0 0 8px 0;">
  <%= data_freshness_label(timestamp) %>
</p>
