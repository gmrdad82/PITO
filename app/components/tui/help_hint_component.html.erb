<%# Beta 4 — Phase F1. ? help hint. Right-side BSB hint.
    Key glyph in foreground; label in muted. Not italic. %>
<span class="bsb-hint-key"><%= help_key %></span><span class="bsb-hint-label"> <%= help_label %></span>
