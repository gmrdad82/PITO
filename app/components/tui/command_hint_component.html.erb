<%# Beta 4 — Phase F1. : command hint. Right-side BSB hint.
    Key glyph in foreground; label in muted. Not italic. %>
<span class="bsb-hint-key"><%= command_key %></span><span class="bsb-hint-label"> <%= command_label %></span>
