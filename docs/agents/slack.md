# pito-slack project stub

The `pito-slack` agent reads this file at dispatch time.

## Channel
- `default_channel: "#dev"` — private channel; agent uses `slack_search_channels` with `channel_types: "private_channel"` to resolve the channel ID at send time.
