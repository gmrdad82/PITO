# Pito API base URL — defaults to https://app.pitomd.com
# Override for local dev, staging, or future environments
# PITO_API_URL=https://app.pitomd.com

# Set to "yes" to force the offline mock client (useful for dev without network)
# PITO_USE_MOCK=no
